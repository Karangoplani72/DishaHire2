
{
  "name": "deprecated-backend",
  "private": true,
  "scripts": {
    "build": "echo 'Redirecting build to root...' && cd .. && npm run build",
    "start": "echo 'Redirecting start to root...' && cd .. && npm start"
  }
}
